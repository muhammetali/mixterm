name: mixterm
base: core22
version: '1.0.0'
summary: Professional SSH/SFTP client
description: |
  MixTerm is a professional SSH/SFTP client with multi-tab terminal
  sessions, a visual SFTP file browser, AES-256 encrypted credential
  storage, and Google Drive cloud sync.
grade: stable
confinement: strict

apps:
  mixterm:
    command: usr/bin/mixterm
    extensions: [gnome]
    plugs:
      - network
      - network-bind
      - home
      - removable-media
    desktop: usr/share/applications/com.mixterm.mixterm.desktop

parts:
  mixterm:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart
    build-packages:
      - clang
      - cmake
      - ninja-build
      - pkg-config
      - libgtk-3-dev
    stage-packages:
      - libgtk-3-0
    organize:
      bundle/mixterm: usr/bin/mixterm
      bundle/*: usr/bin/
    override-build: |
      craftctl default
      mkdir -p $CRAFT_PART_INSTALL/usr/share/applications
      sed 's|Exec=mixterm|Exec=mixterm|' \
        $CRAFT_PART_SRC/linux/runner/mixterm.desktop \
        > $CRAFT_PART_INSTALL/usr/share/applications/com.mixterm.mixterm.desktop
      for size in 16 24 32 48 64 128 256 512; do
        dir=$CRAFT_PART_INSTALL/usr/share/icons/hicolor/${size}x${size}/apps
        mkdir -p "$dir"
        cp $CRAFT_PART_SRC/assets/icons/linux/mixterm_${size}.png "$dir/mixterm.png"
      done
